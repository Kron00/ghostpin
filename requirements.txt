# Pinned exactly. Unbounded ranges mean a future release is picked up silently
# on every fresh install and in CI; these are the versions Ghostpin 2.0.0 is
# built and tested against. Dependabot proposes bumps as reviewable PRs.
pymobiledevice3==10.3.1
flask==3.1.3
requests==2.34.2
pywebview==6.2.1
